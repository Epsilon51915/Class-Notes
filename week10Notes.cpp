#include <iostream>

using namespace std;

int main(){

    /*
        Default constructors:
        In the absence of a user made constructor, the compiler will provide you, though you can't
        guarantee what the initial values of variables are.
    */

    return 0;
}